---
const { pathname } = Astro.url;
const normalizedPathname = pathname.replace(/\/$/, "");

const navLinks = [
  { href: "/blog", label: "Blog" },
  { href: "/contact", label: "Contact" },
];
---

<nav class="fixed top-0 w-full z-50 bg-background/60 backdrop-blur-xl border-b border-white/5">
    <div class="flex justify-between items-center px-6 md:px-8 py-4 md:py-6 max-w-7xl mx-auto w-full">
      <a href="/" class="block">
        <img 
            src="/images/screen2.png"
            alt="VBTecH Solutions Logo"
            class="h-10 md:h-20 w-auto object-contain transition-transform duration-300 ease-in-out hover:scale-110"
        />
      </a>

        <div class="flex gap-4 md:gap-8 items-center">
            {navLinks.map(({ href, label }) => {
                const isActive = normalizedPathname === href;
                return (
                    <a
                        class={`font-headline uppercase tracking-widest text-[10px] md:text-sm transition-colors duration-300 active:duration-100 ${
                            isActive 
                            ? "text-primary border-b-2 border-primary pb-1" 
                            : "text-white/70 hover:text-primary"
                        }`}
                        href={href}
                    >
                        {label}
                    </a>
                );
            })}
        </div>
    </div>
</nav>
